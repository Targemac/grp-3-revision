let initialValue = 0;

let products = ["apple", "berries", "banana", "carrots"];

const num_of_fruits = products.length;

while (initialValue < num_of_fruits) {
  // perfomr the ff ops

  console.log(products[initialValue]);

    initialValue++;
    // initialValue = initialValue + 1;
}
